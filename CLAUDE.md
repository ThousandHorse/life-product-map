@AGENTS.md

## 許可済みコマンド

以下のコマンドは実行時に確認不要（自動許可）:

- `gh api repos/*/pulls/*/comments/*/replies` — PR レビューコメントへのリプライ
- `gh api repos/*/pulls/*/reviews/*/comments` — PR レビューコメントの取得
- `gh pr view` / `gh pr list` — PR 情報の参照（読み取りのみ）
